namespace Microsoft.eShopOnContainers.Services.Ordering.Domain.Seedwork
{
    /// <summary>
    /// The class is decorated with an interface named IAggregateRoot. That interface is an empty interface, sometimes called a /i marker interface /i,
    /// that is used just to indicate that this entity class is also an aggregate root.
    /// </summary>
    public interface IAggregateRoot { }
}
